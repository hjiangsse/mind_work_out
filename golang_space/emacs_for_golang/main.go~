package main

import (
	"fmt"
	"strconv"
)

func test_godef() {
	fmt.Println("This is a call to test_godef")
}

type Student struct {
	Name     string
	Age      int
	Position string
}

func main() {
	hello := 123
	hi := "This is a line"
	fmt.Println(hello)
	fmt.Println(hi)
	test_godef()

	i, err := strconv.Atoi("-42")
	if err != nil {
		fmt.Println(i)
	}
}
